name: Publishing disabled

# This repository intentionally has no publishing trigger or token-bearing job.
# Release to GitHub, Modrinth, or CurseForge requires separate explicit approval.
on:
  workflow_dispatch:

permissions: {}

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publishing is intentionally disabled for this port.'
